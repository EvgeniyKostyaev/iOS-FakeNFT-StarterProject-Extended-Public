Андрианов Дмитрий Владиславович

Когорта: 33

Группа: 1

Эпик: Профиль

Ссылка на доску: https://github.com/users/EvgeniyKostyaev/projects/3/views/7

---

# Декомпозиция эпика Профиль


# Модуль 1 — главный экран профиля (est: ~6.5–8 ч)

## Главный экран профиля

#### Верстка

**Экран профиля (каркас и данные)**

- Создать `ProfileView` в SwiftUI  
- Добавить `NavigationStack` для потока профиля  
- Разметить шапку: аватар (AsyncImage / Kingfisher)  
- Отобразить имя и описание (многострочный текст)  
- Блок «Сайт»: текст ссылки или кнопка, подготовка URL для WebView  

est: 2 часа; fact: 3.4 часа

**Кнопка редактирования**

- Добавить `ToolbarItem` / кнопку в навбаре 

est: 0.5 часа; fact: 0.3 часа

#### Навигация

**Переход на экран редактирования**

- `NavigationLink` или `navigationDestination` на `ProfileEditView` (заглушка допустима на этапе модуля 1)  

est: 0.5 часа; fact: 0.2 часа

**Переход на экран «Мои NFT»**

- Строка в `List` + переход на `MyNFTView` (заглушка или пустой экран)  

est: 0.5 часа; fact: 0.5 часа

**Переход на экран «Избранные NFT»**

- Строка в `List` + переход на `FavoriteNFTView` (заглушка или пустой экран)  

est: 0.5 часа; fact: 0.4 часа

**Открытие сайта в WebView**

- Отдельный `WebViewRepresentable` (SwiftUI)  
- Внутри: `UIViewRepresentable` + `WKWebView`  
- Передача `URL` из модели профиля  

est: 1.5 часа; fact: 2.2 часа


#### Логика (MVVM)

**ProfileViewModel**

- Класс / `@Observable` или `ObservableObject` + `@Published`  
- Состояния: `idle` / `loading` / `loaded` / `failed`  
- Метод `loadProfile()` при `onAppear`  

est: 1.5 часа; fact: 2 часа

#### Работа с сетью (подключение к экрану)

**Получение профиля с API**

- Использовать существующий `NetworkClient`  
- Запрос GET профиля (путь и заголовок токена по API)  
- Декодирование в модель профиля  
- Проброс ошибки во ViewModel для отображения на UI  

est: 1.5 часа; fact: 2.3 часа

**Индикатор загрузки и ошибка на главном экране**

- `ProgressView` при `loading`  
- Текст / кнопка «Повторить» при `failed` (по макету или минимально)  

est: 0.5 часа; fact: 0.5 часа

---

# Модуль 2 — редактирование профиля (est: ~9.5–12 ч)

## Редактирование профиля

#### Верстка

**Форма редактирования**

- Создать `ProfileEditView`  
- Поля: имя (`TextField`), описание (`TextField`/`TextEditor`), сайт (`TextField` с клавиатурой URL)  
- Разметка на `Form` или `VStack` + отступы по привычке проекта  

est: 2 часа; fact: x часов

**Кнопки и выход с экрана**

- Кнопка «Сохранить» внизу экрана  
- Выход через системную кнопку «Назад»; при несохранённых изменениях — алерт «Уверены, что хотите выйти?» («Остаться» / «Выйти»)  

est: 1 час; fact: x часов

**Выбор / смена аватара**

- `PhotosPicker` или аналог
- Превью выбранного изображения  
- Подготовка данных для отправки на сервер (если API принимает файл/base64/url — по факту API)  

est: 2.5 часа; fact: x часов

#### Логика

**ProfileEditViewModel**

- Хранение черновика полей (имя, описание, сайт, фото)  
- Флаг `isSaving`, отключение кнопки при сохранении  

est: 1 час; fact: x часов

**Отображение текущих данных профиля**

- Инициализация формы переданным профилем или повторная загрузка по id  
- Заполнение полей при открытии экрана  

est: 0.5 часа; fact: x часов

**Валидация**

- Минимальные правила (непустое имя, корректность URL сайта)  
- Показ алерта или inline-ошибки  

est: 0.5 часа; fact: x часов

**Сохранение и обновление главного экрана**

- Вызов сервиса PUT после нажатия «Сохранить»  
- По успеху: `dismiss` + обновление данных на `ProfileView` (через обновлённый объект в родителе, повторный `loadProfile()`, или общий стор)  

est: 1.5 часа; fact: x часов

#### Работа с сетью

**Отправка обновлённых данных профиля (PUT)**

- Сбор тела запроса по контракту API  
- Обработка ответа и ошибок  

est: 1 час; fact: x часов

**Загрузка / обновление аватара на сервере**

- Отдельный запрос или часть PUT — по Postman/API  
- Связка с UI после успеха  

est: 1.5 часа; fact: x часов

---

# Модуль 3 — Мои NFT и избранные (est: ~15–18 ч)

## Мои NFT

#### Верстка

**Экран и список**

- Создать `MyNFTView` + `NavigationStack` при необходимости  
- `List` с идентификаторами строк  
- Ячейка: иконка NFT, название, автор, цена в ETH (`HStack` / кастомная строка)  

est: 2.5 часа; fact: x часов

**Кнопка сортировки**

- `ToolbarItem` с `Menu` или `confirmationDialog`  
- Пункты сортировки: по цене / рейтингу / названию

est: 1 час; fact: x часов

**Пустое состояние**

- Условный вид при пустом массиве: текст «У вас ещё нет NFT»

est: 0.5 часа; fact: x часов

#### Логика

**MyNFTViewModel**

- `@Published` массив элементов для списка  
- Метод применения сортировки к массиву  
- Сохранение выбранного критерия в `@AppStorage` или `UserDefaults`  
- Значение по умолчанию — по рейтингу

est: 2.5 часа; fact: x часов

#### Работа с сетью

**Получение списка NFT пользователя**

- Запрос к API или чтение из модели профиля
- Маппинг в модель ячейки списка  

est: 1.5 часа; fact: x часов

---

## Избранные NFT

#### Верстка

**Сетка**

- Создать `FavoriteNFTView`  
- `LazyVGrid` с `GridItem` (2 колонки или по Figma)  
- Адаптивные отступы между ячейками  

est: 1.5 часа; fact: x часов

**Ячейка избранного**

- Создать `FavoriteNFTCellView`  
- Изображение, название, рейтинг, цена в ETH  
- Кнопка-сердце (снятие из избранного)  

est: 2.5 часа; fact: x часов

**Пустое состояние**

- Текст «У вас ещё нет избранных NFT» при пустом списке  

est: 0.5 часа; fact: x часов

#### Логика

**FavoriteNFTViewModel**

- Загрузка списка избранного  
- Метод `removeFromFavorites(id:)` после успешного ответа API обновить список

est: 1.5 часа; fact: x часов

**Обновление UI после удаления из избранного**

- Удаление элемента из `@Published` массива после успешного ответа API  
- Обработка ошибки 

est: 1 час; fact: x часов

#### Работа с сетью

**Получение избранных NFT**

- Запрос GET или данные из профиля — по API  

est: 1 час; fact: x часов

**Удаление из избранного (API)**

- Обновление избранного: PUT профиля с полем likes (список id после удаления)»

est: 1.5 часа; fact: x часов
